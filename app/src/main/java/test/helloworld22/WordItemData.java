package test.helloworld22;

import android.support.v7.app.AppCompatActivity;

public class WordItemData  extends AppCompatActivity {
    public String no;
    public String name;
    public String phone;

    // 화면에 표시될 문자열 초기화
    public WordItemData(String no, String name, String phone) {
        this.no = no;
        this.name = name;
        this.phone = phone;
    }
}
    // 입력받은 숫자의 리스트생성
 //   public static ArrayList<WordItemData> createContactsList(int numContacts) {
      //  ArrayList<WordItemData> contacts = new ArrayList<WordItemData>();
       // for(int i=0; i < jarray.length(); i++){

        //    contacts.add(new WordItemData(temp_no,temp_name,temp_phone));
 //       }
       // for (int i = 1; i <= numContacts-2; i++) {
        //    contacts.add(new WordItemData("2 ", "Jung","324324"));
       // return contacts;
 //   }


